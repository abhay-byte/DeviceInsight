<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Required Permissions -->
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:name=".SystemStatsApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SystemStats"
        tools:targetApi="31">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.SystemStats" />

        <!-- Launcher entry points are activity-aliases so the in-app media pick can swap
             the icon (enable-new-FIRST-then-disable-others — never zero enabled LAUNCHERs). -->
        <activity-alias
            android:name=".MainActivityPaper"
            android:enabled="true"
            android:exported="true"
            android:icon="@mipmap/ic_launcher_paper"
            android:roundIcon="@mipmap/ic_launcher_paper"
            android:targetActivity=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <activity-alias
            android:name=".MainActivityCarbon"
            android:enabled="false"
            android:exported="true"
            android:icon="@mipmap/ic_launcher_carbon"
            android:roundIcon="@mipmap/ic_launcher_carbon"
            android:targetActivity=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <activity-alias
            android:name=".MainActivityBlueprint"
            android:enabled="false"
            android:exported="true"
            android:icon="@mipmap/ic_launcher_blueprint"
            android:roundIcon="@mipmap/ic_launcher_blueprint"
            android:targetActivity=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <activity
            android:name=".ui.caliper.widget.BenchConfigActivity"
            android:exported="true"
            android:theme="@style/Theme.SystemStats">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
        </activity>

        <activity
            android:name=".presentation.settings.SettingsActivity"
            android:exported="false"
            android:theme="@style/Theme.SystemStats" />

        <service
            android:name=".service.OverlayService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Performance monitoring overlay" />
        </service>

        <!-- M2: Quick Settings tile — cycles CALIPER media (shares "caliper" DataStore). -->
        <service
            android:name=".service.MediaTileService"
            android:exported="true"
            android:label="@string/app_name"
            android:icon="@drawable/ic_tile_caliper"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <!-- BENCH widgets — 5 instruments (S-12) -->
        <receiver
            android:name=".ui.caliper.widget.SingleChannelWidgetReceiver"
            android:exported="true"
            android:label="SCOPE">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/single_channel_widget_info" />
        </receiver>

        <receiver
            android:name=".ui.caliper.widget.DualChannelWidgetReceiver"
            android:exported="true"
            android:label="STACK">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/dual_channel_widget_info" />
        </receiver>

        <receiver
            android:name=".ui.caliper.widget.BenchWidgetReceiver"
            android:exported="true"
            android:label="BENCH">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/bench_widget_info" />
        </receiver>

        <receiver
            android:name=".ui.caliper.widget.FuelWidgetReceiver"
            android:exported="true"
            android:label="FUEL">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/fuel_widget_info" />
        </receiver>

        <receiver
            android:name=".ui.caliper.widget.RasterWidgetReceiver"
            android:exported="true"
            android:label="RASTER">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/raster_widget_info" />
        </receiver>

        <!-- pin-to-home success callback (log only; sheet refresh is delay+ON_RESUME) -->
        <receiver
            android:name=".ui.caliper.widget.PinSuccessReceiver"
            android:exported="false" />

    </application>

</manifest>
