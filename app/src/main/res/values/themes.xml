<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- CALIPER base theme — framework Material (no appcompat dep), Compose owns the UI. -->
    <style name="Base.Theme.SystemStats" parent="android:Theme.Material.NoActionBar">
        <item name="android:colorBackground">@color/black</item>
        <item name="android:statusBarColor">@color/black</item>
        <item name="android:navigationBarColor">@color/black</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <style name="Theme.SystemStats" parent="Base.Theme.SystemStats" />
</resources>