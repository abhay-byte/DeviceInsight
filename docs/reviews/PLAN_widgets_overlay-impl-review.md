# Impl Review: PLAN_widgets_overlay — PASS 2 ITER 2

## Verdict: REVISE
## Counts: CRITICAL 0 MAJOR 4 MINOR 3

### Findings

#### [MAJOR][CODE_DEFECT] Legacy overlay_prefs still read by OverlayViewModel — grep not zero → FIX_ROUTE: WORKER
- Location: `app/src/main/java/com/ivarna/deviceinsight/presentation/overlay/OverlayViewModel.kt:57`, `app/src/main/java/com/ivarna/deviceinsight/SystemStatsApplication.kt:40`
- Problem: Plan §0.9 pinned single source `caliper` DataStore and acceptance `grep overlay_prefs == 0` (except migration). Migration in `SystemStatsApplication.onCreate` (`SystemStatsApplication.kt:36-80`) correctly copies `fps_mode`→`fpsMode`, maps `showFps|showCpu|showRam|showSwap|showPower|showBattery|showNetwork|showCpuGraph|showPowerGraph`→`hudModules` CSV and clears legacy `overlay_prefs`. However `OverlayViewModel.kt:57` still does `context.getSharedPreferences("overlay_prefs", MODE_PRIVATE)` and `loadInitialState()` (`:69-95`) + `savePreferences()` (`:167-179`) read/write `show*`, `scaleFactor`, `isHorizontal`, `fps_mode`, `metricOrder` exclusively from `overlay_prefs`. After migration the prefs are cleared, so VM re-initializes defaults and never sources `CaliperKeys.hud*`. HudSettingsCache is fed by `caliperDataStore`, but this VM bypasses it, reintroducing dual-store.
- Evidence: `grep -rn overlay_prefs app/src → 2 hits: SystemStatsApplication.kt:40 (migration) + OverlayViewModel.kt:57 (consumer). Plan required 0 after migration. FpsMonitor no longer reads overlay_prefs — correctly uses `HudSettingsCache` (`data/fps/FpsMonitor.kt:16-18,35-36`), but VM does not.`
- Impact: HUD config edited via OverlayScreen toggles write to orphan store, not `caliper`; post-upgrade START/STOP via `buildServiceIntent()` (`OverlayViewModel.kt:182-193`) still puts extras from stale VM; DataStore never updated from that UI.
- Required worker change: Migrate `OverlayViewModel` to read/write `CaliperKeys` via `caliperDataStore` flows (`hudMedium|hudScale|hudOpacity|hudBlur|hudLocked|hudModules|hudShowCoreBank|hudX|hudY|fpsMode`) or deprecate VM behind HUD PR. Achieve `grep overlay_prefs` == 1 (migration comment) or 0 after final PR. Must not `clear()` then still read cleared file in same release without second source.

#### [MAJOR][CODE_DEFECT] MemInfoParser not integrated into foreground pipeline — STACK hatch divergence → FIX_ROUTE: WORKER
- Location: `app/src/main/java/com/ivarna/deviceinsight/data/monitor/MemInfoParser.kt:19-102` vs `app/src/main/java/com/ivarna/deviceinsight/data/repository/DashboardRepositoryImpl.kt:114-126,250-286` vs `app/src/main/java/com/ivarna/deviceinsight/ui/caliper/widget/BenchModel.kt:180-230`
- Problem: `MemInfoParser.parse()` correctly implements plan §0.4: MemTotal denominator for ALL, `swapF=max(0,swapRawF-zramF)`, `freeF=1-(activeF+cachedF+zramF+swapF) coerceAtLeast 0`, normalization if sum>1 (`:60-73`), secondary sanity `abs(total-1)>0.02` (`:92-99`), order `[active SOLID CH-02, cached DIAGONAL CH-03, zram CROSS CH-04, swap CROSS only if zramF==0&&swapF>0.001, free NONE]` (`:77-90`), fallback single SOLID when missing MemTotal (`:104-115`). `BenchBudgetWorker.kt:21-27` correctly enriches BUDGET snapshot via `MemInfoParser.readMeminfoString()`/`readZramBytes()`/`parse()`. However `DashboardRepositoryImpl.collectMetrics()` never calls `MemInfoParser`; `toBenchSnapshot()` (`BenchModel.kt:180-230`) still builds composition via `usedFraction=ramUsed/ramTotal`, `swapRawF=swapUsed/ramTotal`, `freeF=1-used-swap` normalize. Hence foreground vs BUDGET diverge — violates plan §0.7 "pin sampler to reuse MemInfoParser so foreground vs BUDGET hatch bar does not diverge" and §0.4 "Caller MemoryProvider or DashboardRepositoryImpl reads file, passes string".
- Evidence: `grep MemInfoParser data/repository/DashboardRepositoryImpl.kt → 0 hits; grep BenchBudgetWorker.kt → hits at :8,21,23; toBenchSnapshot at BenchModel.kt:203-210 shows simple math, comment "Corrected fractions ... Keep fallback to simple used/swap/free when Active/Cached not available" leaving cadastral unused.`
- Impact: STACK memory bar foreground shows 2-seg used+swap+free, not Active/Cached/ZRAM cadastral. Acceptance harness `activeF+cachedF+zramF+swapF+freeF sums 1±0.02` cannot pass for foreground. `BenchArt.hatchBar` still `coerceAtMost(w)` hides overflow but source wrong.
- Required worker change: In `DashboardRepositoryImpl.collectMetrics()`, read `/proc/meminfo` string + `MemInfoParser.readZramBytes()`, call `MemInfoParser.parse()` and pass `memComposition` to `toBenchSnapshot` (extend param or set via copy). Keep parser pure for unit test; fallback only on parse failure (missing MemTotal).

#### [MAJOR][CODE_DEFECT] TopConsumersProvider exists but dead — STACK ledger still via ActivityManager → FIX_ROUTE: WORKER
- Location: `app/src/main/java/com/ivarna/deviceinsight/data/monitor/TopConsumersProvider.kt:13-24` vs `app/src/main/java/com/ivarna/deviceinsight/data/repository/DashboardRepositoryImpl.kt:272-286` vs `app/src/main/java/com/ivarna/deviceinsight/ui/caliper/widget/BenchGlance.kt:337-339,635-644`
- Problem: `TopConsumersProvider.loadTopConsumers(max=5)` correctly does `if (!taskRepository.hasUsageStatsPermission()) return emptyList()` (`:14`) then `taskRepository.getRunningProcesses().take(max).map{Consumer(pkg,label.take(16),rssMb=0)}` (`:16-22`) per plan §0.9 (honest empty-hide, label-only when RSS unavailable). File exists and is injectable. However no caller uses it: `DashboardRepositoryImpl.toBenchSnapshot` is invoked with `consumers=emptyList()` default and never injects provider; `BenchGlance StackWidget` at `:337-339` does `if (snap.topConsumers.isEmpty()) loadConsumers(context)` which calls `loadConsumers` at `:635-644` using `ActivityManager.runningAppProcesses` (empty for 3p on API26+ per plan note `BenchGlance.kt:614 RSS=0`) not `TaskRepository`. Thus permission-gated path never exercised; ledger shows fake ActivityManager list or empty regardless of usage-stats grant.
- Evidence: `grep TopConsumersProvider app/src/main/java/com/ivarna/deviceinsight/data/repository → 0; grep BenchGlance loadConsumers → ActivityManager path at 637-640; BenchBudgetWorker comment BUDGET topConsumers empty (intentionally lossy) but foreground also empty. Provider is otherwise unreferenced.`
- Impact: Violates MAJOR 3 fix; Stack T3 ledger hide-vs-show logic untested; user granting usage stats sees no change; plan required empty list to hide ledger (`BenchSnapshot.topConsumers empty → STACK hides ledger rows`).
- Required worker change: Inject `TopConsumersProvider` into `DashboardRepositoryImpl` (or `MonitorBus` enrichment) and call `loadTopConsumers()` to populate `BenchSnapshot.topConsumers` before `monitorBus.pushSlow`; update `StackWidget` to render ledger only from `snap.topConsumers` (already present) and hide section when `empty` (no fallback ActivityManager call), with label-only rows when `rssMb==0`.

#### [MAJOR][MISSING_TEST] Phase 0 verification harness absent → FIX_ROUTE: WORKER
- Location: `app/src/test/java/com/ivarna/deviceinsight/ui/caliper/widget/BenchSelfCheckTest.kt:1-115`, `app/src/test/java/com/ivarna/deviceinsight/CaliperSelfCheckTest.kt:77-93`, no `MemInfoParserTest`
- Problem: Plan §7 verification per phase requires unit tests for: `MemInfoParser` fixture (sum 1±0.02, dedup, normalization, free never negative, hatchBar x+segW≤w), `toBenchSnapshot` mapping of `currentMa/gpuHist/netHist/batteryPresent/cycleCount null`, `stale/warning/Tier`, `cadenceMs LIVE/AMBIENT/BUDGET=15*60_000`, `FpsSample` source in `{SF,GFX,—}`, `HudConfig` CSV round-trip + `HudMedium` mapping + `HudSettingsCache`, `caliperRailOrder`, optional `WidgetReceiversExistTest`. Current `BenchSelfCheckTest` covers `zeroSnapshotIsStale`, `freshNotStale`, `toBenchSnapshotMapping` (cpuPct/watts/batteryPct/net/storage/gpuFitted but not currentMa/remainingMin/batteryPresent/cycle), `tierOf`, `blueprintPaletteIsInk`, `benchConfigDefaults`, `emptyCpuHist`, `warningAndStaleLogic`. No parser test; `grep MemInfoParser app/src/test → 0`; `grep cadenceMs app/src/test → 0` after BUDGET change; `CaliperSelfCheckTest` only expands key list to 15 but no hudMedium mapping test.
- Evidence: `read BenchSelfCheckTest.kt 115 lines — no MemInfoParser import, no cadence BUDGET assertion, no TopConsumers test, no FpsSample test; CaliperSelfCheckTest.kt:92 asserts 15 exact keys (passes) but no behavior tests for migration.`
- Impact: Regressions cited in REMAINING_RISKS slipped (parser not integrated, provider not wired) because missing tests didn't fail. Plan acceptance explicitly listed these tests.
- Required worker change: Add `MemInfoParserTest` with fixtures (normal, Active missing fallback, ZRAM dedup, sum>1 normalization, missing MemTotal fallback, swap-without-zram vs with-zram bar), extend `BenchSelfCheckTest` for `cadenceMs BUDGET == 900000` (15min), `currentMa/batteryPresent` mapping, `BenchFrames` no-recycle, `placedAt` write-once (via in-memory prefs mock), `HudMedium` distinct enum mapping, `BenchBudget` KEEP semantics. Keep `WidgetReceiversExistTest` optional but include if minSdk rename guard desired.

#### [MINOR][CODE_DEFECT] Hairline 4-side frame degraded to fixed 80dp sides, defaultWeight no-op → FIX_ROUTE: WORKER (PLAN_GAP noted)
- Location: `app/src/main/java/com/ivarna/deviceinsight/ui/caliper/widget/BenchGlance.kt:59,168-194`
- Problem: Plan §1.2 pins exact Glance hierarchy `Box(fillMaxSize.bg(panel)){Column(fillMaxSize.padding12){1dp top; Row(fillMaxWidth.defaultWeight()){1dp left fillMaxHeight; Column(defaultWeight pad9){content};1dp right};1dp bottom}}` with verification that `GlanceModifier.defaultWeight()` exists as member of `RowScope`/`ColumnScope` in `glance-1.1.0.aar`. Worker added no-op extension `private fun GlanceModifier.defaultWeight():GlanceModifier=this` at `:59` and implemented `Row(modifier=fillMaxWidth)` without weight (`:184`) and side Boxes `height(80.dp)` not `fillMaxHeight` (`:185,189`), inner Column `fillMaxWidth` not `defaultWeight` (`:186`). Comment acknowledges "Real Glance RowScope/ColumnScope.defaultWeight is member-only; this local extension keeps T1 layout functional."
- Evidence: `read BenchGlance.kt:59 no-op, 184 Row without defaultWeight, 185 side Box height 80dp vs plan fillMaxHeight+weight; git diff shows same; worker handoff notes "defaultWeight fallback is no-op → side hairline height may clip on tall BENCH T5". Plan's RowScope member vs GlanceModifier extension mismatch suspected PLAN_GAP.`
- Impact: 4-side frame shows but sides 80dp tall, not full panel height; on BENCH T5 280dp bottom gap; T1 140dp still 4-side visible so passes cream-on-cream legibility but not stretched height.
- Required worker change: Use scope-aware weight: `Row{ Box(modifier=GlanceModifier.width(1.dp).fillMaxHeight()...) }` inside `RowScope` the `fillMaxHeight` already stretches; if weight needed for height, use `GlanceModifier.weight(1f)` via `RowScope` or keep Column weight pattern: `Column(modifier=GlanceModifier.fillMaxSize().padding(12.dp)){ Box(top); Row(modifier=GlanceModifier.defaultWeight()){...} ; Box(bottom)}` where `defaultWeight` is called with `RowScope` receiver — need `with(RowScope){}` or change import to `androidx.glance.layout.RowScope`. If member-only limitation truly prevents compile, document as known limitation and expand side height to `fillMaxHeight` without weight and verify screenshot on T5; or treat as PLAN_GAP (plan snippet assumed GlanceModifier extension, real is scope member).

#### [MINOR][CODE_DEFECT] FUEL T4 health/cycle/design rows not wired → FIX_ROUTE: WORKER
- Location: `app/src/main/java/com/ivarna/deviceinsight/ui/caliper/widget/BenchGlance.kt:388-445`, `app/src/main/java/com/ivarna/deviceinsight/data/repository/DashboardRepositoryImpl.kt:272-286`, `app/src/main/java/com/ivarna/deviceinsight/data/provider/BatteryProvider.kt:50-76`
- Problem: `BenchSnapshot` has `batteryHealth:String?, cycleCount:Int?, designMah:Int?` (`BenchModel.kt:32-35`). `BatteryProvider.getBatteryDetailedInfo()` at `:50-76` now correctly returns `chargingCycles = if(cycles>=0) cycles else -1` (fixing old 835 leak) and `currentNow`, `chargeCounter`, but `DashboardRepositoryImpl.collectMetrics()` at `:258-286` only populates `currentMa`, `remainingMin`, `batteryPresent`, `gpuHist`, `netHist` via `copy()` — omits `batteryHealth/cycleCount/designMah`. `FuelWidget` at `:388-445` renders only `voltage·currentMa` subline, never T4 rows for health/cycles/design. Plan §0.6 + §1.6 FUEL T4 requires pulling `BatteryProvider.getBatteryDetailedInfo()` when tier≥T4 else omit, never fake 835; hide row when cycle==-1.
- Evidence: `read DashboardRepositoryImpl.kt:280-286 copy fields excludes batteryHealth/cycleCount/designMah; read FuelWidget subline at :436-440 builds voltage·currentMa only; BatteryProvider.kt:75 sentinel -1 correctly.`
- Impact: FUEL T4 shows incomplete datasheet; -1 sentinel correctly not leaked but UI hide-not-show not exercised; spec compliance low but not functional break.
- Required worker change: In `DashboardRepositoryImpl`, call `batteryProvider.getBatteryDetailedInfo()` and map `batteryHealth/batteryLevel/cycleCount/designMah` to snapshot; extend `FuelWidget` to render T4 rows only when `tier>=T4` and values non-null/non--1.

#### [MINOR][MISSING_TEST] Picker previewImage still launcher icon — 15 WEBP not shipped → FIX_ROUTE: WORKER
- Location: `app/src/main/res/xml/single_channel_widget_info.xml:13`, `dual_channel_widget_info.xml:13`, `bench_widget_info.xml:13`, `fuel_widget_info.xml:13`, `raster_widget_info.xml:13`
- Problem: All five providers still `android:previewImage="@mipmap/ic_launcher"` per diff `updatePeriodMillis 1800000→0` done but previewImage unchanged. Plan §1.10 requires 15 static WEBP `drawable-nodpi/preview_{scope,stack,fuel,raster,bench}_{paper,carbon,blueprint}.webp` and XML per-kind `previewImage` referencing drawable. Research in plan correctly verified `providePreview` does NOT exist in Glance 1.1.0 (AAR grep 0 matches) so `previewImage` is mandatory. Worker marks as REMAINING_RISKS.
- Evidence: `git diff --cached -- res/xml → all previewImage still @mipmap/ic_launcher; glob drawable-nodpi/preview_*.webp → 0; worker handoff NOT_VERIFIED line "15 picker preview WEBP not generated (previewImage still @mipmap/ic_launcher)".`
- Impact: Picker shows launcher icon not CALIPER panel; Play listing degraded but widgets function. Plan allowed previews as last commit of Plan A — not blocking APPROVE but acceptance includes five picker entries with non-launcher previews.
- Required worker change: Generate WEBP via debug Activity/screenshot test rendering `PreviewPanel` per kind×medium (paper/carbon/blueprint) with CALIPER panels, -3° tilt, drop shadow only per design §8, place under `drawable-nodpi` and update XML `previewImage`. Keep `providePreview` not implemented per AAR verification.

## Evidence
- git status: `On branch master, Changes to be committed: 39 files 3176+/510- (staged app/)`, `git diff --cached --stat 39 files`, `git log --oneline -5 285e0be fix(caliper): paper theme chrome...`
- `read app/src/main/java/com/ivarna/deviceinsight/data/monitor/MonitorBus.kt:11 @Singleton MonitorBus pushSlow/pushFast/GlobalSnapshot.last`
- `git diff --cached -- BenchModel.kt` shows `cadenceMs BUDGET 15*60_000`, `BenchFrames lru sizeOf /1024` no `entryRemoved recycle`, `BenchState.save` placedAt write-once check `if(p[KEY_PLACED]==null)`, `toBenchSnapshot` fallback composition
- `read MemInfoParser.kt:19-102` exact fractions, `max(0,swapRawF-zramF)`, `freeF coerceAtLeast 0`, normalization
- `read FpsMonitor.kt:13 FpsSample, 126 getCurrentFpsWithSource SF→GFX→—, 191 layer cache 30s, 35-36 HudSettingsCache`
- `read HudSettingsCache.kt:18 @Volatile fpsMode fed by hudFpsModeFlow on IO`
- `read CaliperPrefs.kt:19-35 CaliperKeys 15 keys + flows hudMedium..hudMigrated, 77-102 hud flows`
- `read SystemStatsApplication.kt:27-80 migration + BenchBudget.enqueue on IO`
- `read DashboardRepositoryImpl.kt:72 delay 500, 96-101 gpuHistory/netHistory LinkedList 61, 250-322 pushSlow + HudSlow mapping clusterSizes heuristic, batteryPresent/currentMa/remainingMin, BenchUpdater.nudge`
- `read AppModule.kt:143 monitorBus injection`
- `read BenchGlance.kt:59 defaultWeight no-op, 168-194 hairline hierarchy, 530 ResponsiveBench T2..T5, 644-710 receivers per-id onEnabled/onDisabled IO launch cancelIfNone + onDeleted per-id remove/evict`
- `read BenchBudget.kt:15 15 MINUTES KEEP, 20 suspend cancelIfNone sumOf 5 kinds`
- `read BenchBudgetWorker.kt:17 direct sample never MonitorBus, 23 MemInfoParser enrichment, 63 BenchBudgetSnapshot holder, 51-54 clear after delay`
- `read WidgetsSheet.kt:39-50 Lifecycle ON_RESUME + LaunchedEffect, 170-190 requestPin delay1200`
- `read SettingsScreen.kt:38-64 per-kind counts NOT PLACED/PLACED×N + ON_RESUME observer`
- `read HudTheme.kt:13 distinct HudMedium + mapping, HudModel.kt:11 HudScale/Module/Config`
- `grep overlay_prefs 2 hits, BenchSnapshotCache 0 hits, MonitorBus 5 hits only foreground writer`
- `read BatteryProvider.kt:75 chargingCycles -1 sentinel`
- `read DashboardMetrics.kt:48-49 gpuHistory/netHistory defaults`
- `read BenchArt.kt:96 hatchBar, 119 spark, 285 coreRailRows`
- `read CaliperSelfCheckTest.kt:92 expects 15 keys list — passes; BenchSelfCheckTest.kt 115 lines no parser test`
- `git diff --cached -- AndroidManifest.xml adds 2 receivers FUEL/RASTER + BenchConfigActivity singleTop + widgetCategory/home_screen reconfigurable`
- `git diff --cached -- res/xml 5 files updatePeriodMillis 0 + descriptions widget_desc_*`

## Next Action
WORKER to address 4 MAJORs (overlay_prefs VM, foreground MemInfoParser integration, TopConsumers wiring, harness tests) and 3 MINORs (hairline scope-weight, FUEL T4, previews) then re-request impl-review. No plan rework needed; HUD compose rewrite + FpsTicker 10Hz hosting remain Phase 3 stub per handoff — defer to next HUD PR. Route after worker fix → impl-reviewer PASS 3.
